{
    "actions": [
      {
        "name": "set_jwt_issuer",
        "args": {
          "issuer": "${ISSUER}",
          ${JWKS}
          ${CA_CERT_BUNDLE_NAME_FIELD}
          ${AUTO_REFRESH}
          "key_filter": "all"
        }
      }
    ]
}